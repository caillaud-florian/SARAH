#include "HalfEdge.hpp"

namespace geo
{

	/*============ HalfHalfEdge2D ============*/

	// bool HalfEdge2D::operator ==(HalfEdge2D pHalfEdge)
	// {
	// 	return (this->a == pHalfEdge.a && this->b == pHalfEdge.b);
	// }

	// bool HalfEdge2D::operator !=(HalfEdge2D pHalfEdge)
	// {
	// 	return !(this->a == pHalfEdge.a && this->b == pHalfEdge.b);
	// }

	// ============ HalfHalfEdge ============

	// bool HalfEdge::operator ==(HalfEdge pHalfEdge)
	// {
	// 	return (this->a == pHalfEdge.a && this->b == pHalfEdge.b);
	// }

	// bool HalfEdge::operator !=(HalfEdge pHalfEdge)
	// {
	// 	return !(this->a == pHalfEdge.a && this->b == pHalfEdge.b);
	// }


}